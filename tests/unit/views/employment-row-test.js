import { moduleFor, test } from 'ember-qunit';

moduleFor('view:employment-row', 'Unit | View | employment row');

// Replace this with your real tests.
test('it exists', function(assert) {
  var view = this.subject();
  assert.ok(view);
});
